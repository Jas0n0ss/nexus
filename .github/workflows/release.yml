# ⚠️  DEPRECATED — consolidated into ci.yml
# This file can be safely deleted from the repository.
# It is kept here only because the CI system does not allow in-place deletion.
name: _deprecated-release
on:
  workflow_dispatch:
    inputs:
      note:
        description: 'This workflow is deprecated. Use ci.yml instead.'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. All builds are now handled by ci.yml. Delete this file."
