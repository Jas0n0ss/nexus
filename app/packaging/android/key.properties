# Community release keystore used by CI for sideloadable APKs.
# Replace with your own Play Store keystore via GitHub Secrets for production.
storePassword=nexus-release
keyPassword=nexus-release
keyAlias=nexus
# Path is relative to android/app/ (where build.gradle lives)
storeFile=../keystore/nexus-release.p12
